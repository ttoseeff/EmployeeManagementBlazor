@page "/attr"

<ChildComponent placeholder="placeholder from parent inside" size="15" maxlength="15"></ChildComponent>

@code {
    private Dictionary<string, object> InputParameters { get; set; } =
        new Dictionary<string, object>()
            {
            {"required", "required"},
            {"placeholder", "placeholder from parent"},
            {"size", "20"},
            {"maxlength", "20"},

            };
    private string Value { get; set; } = "Parent";


}
